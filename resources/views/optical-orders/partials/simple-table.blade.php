<div class="table-scroll"><table><thead><tr><th>{{ __('common.number') }}</th><th>{{ __('patients.patient') }}</th><th>{{ __('orders.frame') }}</th><th>{{ __('common.status') }}</th><th>{{ __('orders.expected_delivery') }}</th><th>{{ __('orders.outstanding') }}</th><th>{{ __('common.actions') }}</th></tr></thead><tbody>@forelse($tableOrders as $order)<tr><td><strong>{{ $order->order_number }}</strong><br><span class="table-meta">{{ $order->order_date }}</span></td><td>{{ $order->patient_name }}<br><span class="table-meta">{{ $order->phone ?? $order->whatsapp_number }}</span></td><td>{{ $order->frame_name ?: __('common.not_set') }}</td><td><x-status-badge :status="$order->status" /></td><td>{{ $order->expected_delivery_date }}</td><td>{{ config('erp.module.currency', 'SAR') }} {{ number_format($order->outstanding_amount, 2) }}</td><td><a class="button button-ghost" href="{{ route('optical-orders.app', ['page' => 'details', 'order_id' => $order->id, 'lang' => app()->getLocale()]) }}">{{ __('common.open') }}</a><a class="button button-ghost" href="{{ route('documents.optical-orders.show', ['order' => $order->id, 'lang' => app()->getLocale()]) }}">{{ __('common.print') }}</a><a class="button button-ghost" href="{{ route('documents.optical-orders.glasses-card', ['order' => $order->id, 'lang' => app()->getLocale(), 'format' => 'pdf']) }}">{{ __('Card') }}</a></td></tr>@empty<tr><td colspan="7"><x-empty-state /></td></tr>@endforelse</tbody></table></div>
